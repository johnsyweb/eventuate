#!/usr/bin/env bash
#MISE description="Dry-run semantic-release locally (requires GITHUB_TOKEN)"
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"

aube run release --dry-run
