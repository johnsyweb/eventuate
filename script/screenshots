#!/usr/bin/env bash
#MISE description="Generate Chrome Web Store screenshots"
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

aube run build
aube run screenshots
